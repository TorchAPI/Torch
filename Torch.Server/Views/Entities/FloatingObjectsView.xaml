<UserControl x:Class="Torch.Server.Views.Entities.FloatingObjectsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Torch.Server.Views.Entities"
             xmlns:entities="clr-namespace:Torch.Server.ViewModels.Entities"
             mc:Ignorable="d" Width="auto" Height="auto">
    <UserControl.DataContext>
        <entities:FloatingObjectViewModel/>
    </UserControl.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Label Content="Name" Width="125"/>
            <TextBox Text="{Binding Name, Mode=OneWay}" Margin="3" IsReadOnly="True"/>
        </StackPanel>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Label Content="Amount" Width="125"/>
            <TextBox Text="{Binding Amount, Mode=OneWay}" Margin="3" IsReadOnly="True"/>
        </StackPanel>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <Label Content="Position" Width="125"/>
            <TextBox Text="{Binding Position, Mode=OneWay}" Margin="3" IsReadOnly="True"/>
        </StackPanel>
        
        <StackPanel Grid.Row="3" Orientation="Horizontal">
            <Label Content="EntityID" Width="125"/>
            <TextBox x:Name="EntityIDText" Text="{Binding Id, Mode=OneWay}" Margin="3" IsReadOnly="True"/>
        </StackPanel>
        
        <StackPanel Grid.Row="4" Orientation="Horizontal">
            <Label Content="Speed" Width="125" />
            <TextBox Text="{Binding Speed, Mode=OneWay}" Margin="3" IsReadOnly="True"/>
        </StackPanel>
        
        <StackPanel Grid.Row="5" Orientation="Horizontal">
            <Label Content="Distance From Center" Width="125" />
            <TextBox Text="{Binding PrettyDistance, Mode=OneWay}" Margin="3" IsReadOnly="True"/>
        </StackPanel>
        
        <ScrollViewer Grid.Row="6" Margin="3" VerticalScrollBarVisibility="Auto">
            <local:EntityControlsView DataContext="{Binding}"/>
        </ScrollViewer>
    </Grid>
</UserControl>