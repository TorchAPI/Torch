<UserControl x:Class="Torch.Server.Views.Entities.PlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:entities="clr-namespace:Torch.Server.ViewModels.Entities"
             xmlns:local="clr-namespace:Torch.Server.Views.Entities"
             mc:Ignorable="d">
    <UserControl.DataContext>
        <entities:PlayerViewModel />
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Name" Margin="3"/>
        <TextBox Grid.Row="0" Grid.Column="1" IsReadOnly="True" Text="{Binding Name, Mode=OneWay}"/>
        
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Identity" Margin="3"/>
        <TextBox Grid.Row="1" Grid.Column="1" IsReadOnly="True" Text="{Binding ID, Mode=OneWay}"/>
        
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Steam ID" Margin="3"/>
        <TextBox Grid.Row="2" Grid.Column="1" IsReadOnly="True" Text="{Binding SteamID, Mode=OneWay}"/>
        
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Faction Tag" Margin="3"/>
        <TextBox Grid.Row="3" Grid.Column="1" IsReadOnly="True" Text="{Binding FactionTag, Mode=OneWay}"/>
        
        <TextBlock Grid.Row="4" Grid.Column="0" Text="Faction Name" Margin="3"/>
        <TextBox Grid.Row="4" Grid.Column="1" IsReadOnly="True" Text="{Binding FactionName, Mode=OneWay}"/>
        
        <TextBlock Grid.Row="5" Grid.Column="0" Text="Last Login" Margin="3"/>
        <TextBox Grid.Row="5" Grid.Column="1" IsReadOnly="True" Text="{Binding LastLogin, Mode=OneWay}"/>
        
        <TextBlock Grid.Row="6" Grid.Column="0" Text="Last Logout" Margin="3"/>
        <TextBox Grid.Row="6" Grid.Column="1" IsReadOnly="True" Text="{Binding LastLogout, Mode=OneWay}"/>
        
        <TextBlock Grid.Row="7" Grid.Column="0" Text="Last Death Location" Margin="3"/>
        <TextBox Grid.Row="7" Grid.Column="1" IsReadOnly="True" Text="{Binding LastDeathLocation, Mode=OneWay}"/>
        
        <TextBlock Grid.Row="8" Grid.Column="0" Text="Blocks Built" Margin="3"/>
        <TextBox Grid.Row="8" Grid.Column="1" IsReadOnly="True" Text="{Binding BlocksBuilt, Mode=OneWay}"/>
        
        <TextBlock Grid.Row="9" Grid.Column="0" Text="PCU" Margin="3"/>
        <TextBox Grid.Row="9" Grid.Column="1" IsReadOnly="True" Text="{Binding PCU, Mode=OneWay}"/>
        
        <TextBlock Grid.Row="10" Grid.Column="0" Text="Over Limits" Margin="3"/>
        <TextBox Grid.Row="10" Grid.Column="1" IsReadOnly="True" Text="{Binding OverLimits, Mode=OneWay}"/>
        
        <ScrollViewer Grid.Row="7" Grid.Column="0" Margin="3" VerticalScrollBarVisibility="Auto">
            <local:EntityControlsView DataContext="{Binding}"/>
        </ScrollViewer>
    </Grid>
</UserControl>
